# バドミントン ストリング管理アプリ

## 概要

ラケットのストリングテンションを自動計算・管理するWebアプリ。経過日数・沖縄のリアルタイム気温・湿度を考慮してテンションを推定する。

## 起動方法

```bash
npm run dev
```

## 技術スタック

| 技術 | 用途 |
|------|------|
| React + Vite | フロントエンド |
| Tailwind CSS v4 | スタイリング |
| date-fns | 日付計算 |
| lucide-react | アイコン |
| Web Notifications API | ブラウザ通知 |
| Open-Meteo API | 沖縄リアルタイム気象データ（無料・認証不要） |

## 機能一覧

| 機能 | 詳細 |
|------|------|
| ラケット管理 | 名前・ラケット種類・ストリング種類を登録 |
| テンション記録 | 初期テンション (lbs) を記録 |
| 張り日管理 | 張った日にちを記録 |
| 現在テンション自動計算 | 経過日数・気温・湿度に基づく推定値を表示 |
| 張替え時期 | 目安日数を設定、残日数と進捗バーで可視化 |
| ステータス表示 | 良好 / 交換近し / まもなく交換 / 交換超過 の4段階 |
| ブラウザ通知 | 期限超過 or 7日前になると Web Notification で通知 |
| データ永続化 | LocalStorage に保存（ブラウザリロード後も維持） |

## テンション計算モデル

### 補正要素まとめ

| 要素 | 補正量 |
|------|--------|
| 時間劣化 | 最初7日で −5%、以降30日ごと −3% |
| 気温補正 | −0.1 lbs/°C（基準20°C超過分） |
| 湿度補正 | −0.02 lbs/%（基準50%超過分） |

張り日から今日までの月別加重平均を使って累積熱・湿気ストレスを計算。  
例: 夏（7〜8月）に張ったストリングは平均30°C → 気温だけで **−1.0 lbs** の追加低下。

---

## テンション計算モデルのエビデンス

### 1. 時間劣化（最初7日で −5%、以降30日ごと −3%）

**根拠：ストリングメーカーの公表データ・研究**

- Yonex の技術資料によると、張り直後から24〜48時間でテンションの約10〜15%が失われる（クリープ現象）
- 一般的なナイロン系ストリングで張り後1週間以内に初期テンションの5〜8%低下が報告されている
- その後は緩やかに低下し、月あたり2〜4%程度が経験則として知られている

> ストリングの素材（ナイロン・ポリ・ナチュラルガット）や張りの太さで大きく異なる。現在のモデルは汎用的な中間値。

---

### 2. 気温補正（−0.1 lbs/°C）

**根拠：熱膨張の物理法則 + 業界の経験則**

- ストリングス（ナイロン系）の線膨張係数：約 7〜9 × 10⁻⁵ /°C
- ラケットフレーム（グラファイト）の線膨張係数：約 1〜3 × 10⁻⁶ /°C
- この差により、気温が上がるとストリングだけが伸び、実効テンションが下がる
- 業界の経験則：**「10°F（約5.6°C）で約1 lbs」**（バドミントン業界団体・プロストリンガーの間で広く使用）
- 換算すると約 0.18 lbs/°C だが、長期的な常温計算のため保守的に **0.1 lbs/°C** を採用

---

### 3. 湿度補正（−0.02 lbs/%）

**根拠：ナイロンの吸湿特性**

- ナイロン66（ストリングの主材料）は高湿度環境で吸水率が3〜5%増加する
- 水分を吸収したストリングは弾性率が低下し、実効テンションが下がる
- テニス・バドミントン研究では高湿度（80%以上）で0.5〜1 lbs程度の差が報告されている
- バドミントンストリングはテニスより細く影響が小さいため **0.02 lbs/%** を採用

---

### モデルの限界

| 点 | 実態 |
|----|------|
| モデルの精度 | ±1〜2 lbs 程度の目安。精密計測には張力計が必要 |
| ストリング種別の差 | ポリ系はナイロンより温度の影響が大きい（未対応） |
| 使用頻度 | 打つたびにテンションは落ちる（使用回数は未反映） |
| 個体差 | 同じ条件でも張り方・テンションマシンで変わる |

> 現在のモデルは「感覚的な参考値」として使うのが適切。厳密な管理には実際にテンションゲージで計測することを推奨。

---

## 気象データ仕様

- **データソース**：Open-Meteo API（無料・認証不要）
- **観測地点**：沖縄・那覇（26.21°N, 127.68°E）
- **更新頻度**：30分ごと自動更新
- **フォールバック**：API失敗時は沖縄の月別平均値を使用

### UI表示

- ヘッダーに現在の気温（🌡️）・湿度（💧）を常時表示
- 各ラケットカードに「補正内訳」を折りたたみ表示（平均気温・湿度・各補正量）
